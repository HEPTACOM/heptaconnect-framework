#!/usr/bin/env bash

PACKAGE_NAME=$1
PROJECT_DIRECTORY=$(dirname $0)/../..
SOURCE_DIRECTORY="${PROJECT_DIRECTORY}"
TARGET_DIRECTORY="${PROJECT_DIRECTORY}/src/${PACKAGE_NAME}"

cp "${SOURCE_DIRECTORY}/LICENSE.md" "${TARGET_DIRECTORY}/LICENSE.md"
